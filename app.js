// JavaScript to handle game selection
function startGame(game) {
    if (game === 'snake') {
        alert("Snake game will start soon!");
        // Here, you can redirect to the Snake game page or load the game logic.
    } else if (game === 'frogger') {
        alert("Frogger game will start soon!");
        // Redirect to Frogger or load game logic.
    } else if (game === 'pacman') {
        alert("Pacman game will start soon!");
        // Add Pacman logic or redirection.
    }
}
